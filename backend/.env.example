# Society Management Pro - Environment Configuration Template

# Server Port
PORT=5000

# Environment (development or production)
NODE_ENV=development

# Security
JWT_SECRET=super-secure-secret-key-123
API_KEY=hms-api-key-2024-secure
# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:5000,http://localhost:3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=Yaksh@1419
DB_NAME=society_management
DB_CONNECTION_LIMIT=10
# Set to true in production if connecting to a cloud DB that requires SSL
DB_SSL=false

# Twilio Credentials (Optional - Falls back to console simulation if empty)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_MESSAGING_SERVICE_SID=
